[pytest]
asyncio_mode = auto
# Use function scope to create a fresh loop for each test
asyncio_default_fixture_loop_scope = function
# Disable capturing of stdout/stderr to avoid device errors
addopts = -s --disable-warnings
